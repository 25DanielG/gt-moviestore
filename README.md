# Startup Instructions (Mac)
- Start the virtual python environment: ```python3 -m venv venv```
- Run the virtual python environment: ```source venv/bin/activate```
- Install dependencies: ```pip install django==5.0 pillow```
- Migrate: ```python manage.py migrate```
- Run the server: ```python manage.py runserver```